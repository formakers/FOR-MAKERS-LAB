# GitHub 업로드 예시

프로젝트 폴더의 상위 디렉터리에서:

```bash
git add 2026-08-24-humanoid-human-action-data-collection
git status
git commit -m "Add humanoid human action data collection system guide"
git push origin main
```

이미 Git 저장소 안에서 작업 중인 경우에 사용하는 예시입니다.
